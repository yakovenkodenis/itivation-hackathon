<p><%= t('authentication.teammate.mailer.unlock.hello') %>, <%= @resource.email %>!</p>

<p><%= t('authentication.teammate.mailer.unlock.message') %></p>

<p><%= t('authentication.teammate.mailer.unlock.link') %>:</p>

<p><%= link_to <%= t('authentication.teammate.mailer.unlock.action') %>, unlock_url(@resource, unlock_token: @token) %></p>
