<%= simple_form_for(@team) do |f| %>
    <%= f.error_notification %>
    <%= f.input :name, required: true, label: false, placeholder: 'Team name', input_html: { class: 'form-control' } %>
    <%= f.button :submit, class: 'btn btn-primary btn-block' %>
<% end %>
